# Agent 工作指南

## 核心原则

**workflow enforcement**：scope → research → plan → approval → implement → verify

所有大改动必须遵循上述流程。在编写代码前，必须先完成 Research 和 Plan，并获得用户明确批准。不得根据模糊认可自行进入实施阶段。

对于需要记录 Research / Plan 的改动，在正式 Research 前必须先检索现有记录，并由 Agent 根据语义关系自行判断写入位置。

---

## 改动规模判定

### 小改动

局部、低风险、意图明确、容易回滚，且不涉及以下内容：

* 架构或跨模块设计；
* 公开接口、核心类型或数据结构；
* 依赖增删或升级；
* 构建、部署和运行环境；
* 安全、权限或数据边界；
* 数据迁移；
* 核心业务流程或关键用户行为；
* 存在明显技术取舍的设计决策。

小改动可直接进入 Implement，无需 Research / Plan，但仍必须完成必要验证，并根据实际影响判断是否更新 `CHANGELOG.md`。

### 大改动

满足以下任一条件时，原则上按大改动处理：

* 涉及架构或跨模块设计；
* 修改公开接口、核心类型或数据结构；
* 增删或升级依赖；
* 修改构建、部署、运行环境；
* 涉及安全、权限或数据迁移；
* 修改核心业务流程；
* 影响范围较广或难以回滚；
* 存在多种可行技术方案，需要明确权衡；
* 对现有行为存在较高回归风险。

大改动必须执行：

`ADR lookup → Research → Plan → 用户批准 → Implement → Verify`

改动规模根据**行为影响、风险、可逆性和设计决策复杂度**判断，不单纯根据修改文件数量或代码行数判断。

无法明确判断时，按大改动处理并说明理由。

用户拥有最终决定权，可以主动指定某项改动按小改动或大改动处理。

---

## 文件命名规范

### Research 文件

`<YYYY-MM-DD>-<改动名称>-research.md`

### Plan 文件

`<YYYY-MM-DD>-<改动名称>-plan.md`

示例：

`2026-06-02-auth-migration-research.md`

### 归档目录

所有 Research 和 Plan 文件统一存放在 `adr/` 目录下，并按月份建立子目录：

```text
adr/
└── YYYY-MM/
    ├── YYYY-MM-DD-xxx-research.md
    └── YYYY-MM-DD-xxx-plan.md
```

示例：

`adr/2026-06/2026-06-21-code-viewer-structure-research.md`

---

## 工作流程

### 0. ADR / 历史方案检索

需要执行 Research / Plan 时，必须先根据以下信息检索 `adr/`：

* 当前改动主题；
* 相关业务关键词；
* 核心模块名称；
* 可能涉及的文件路径。

不得只按文件名进行完全匹配，应同时关注历史上语义相关的 Research 和 Plan。

检索完成后：

* 如果存在相关记录，向用户列出候选文件及其与当前需求的关系；
* 如果没有相关记录，明确说明未发现同类 ADR。

随后由 Agent 根据相关性和改动边界自行选择以下一种处理方式：

1. 追加或更新现有 Research / Plan；
2. 使用用户指定的其他现有记录；
3. 新建 Research / Plan。

如果相关记录已经完成、主题不同、追加会造成历史语义混乱，优先新建 Research / Plan。只有在写入位置会影响用户明确指定的文档、历史结论存在冲突，或无法判断复用是否安全时，才需要向用户确认。若用户已经明确指定写入方式，则按用户指定执行。

---

## 1. Research

Research 的目标是理解现状和问题，而不是提前设计完整解决方案。

在 Research 阶段必须深度阅读与需求直接相关的代码和调用链，避免仅根据单个文件推测系统行为。

Research 文件至少应包含：

* 当前需求和研究范围；
* 核心文件及其职责；
* 关键调用链和数据流；
* 当前实现逻辑；
* 已有可复用能力；
* 潜在问题和隐患；
* 与需求相关的约束；
* 尚未确定、需要在 Plan 阶段决策的问题。

Research 阶段原则上不得修改产品代码。

允许执行只读操作，例如：

* 搜索代码；
* 阅读文件；
* 查看 Git 历史；
* 查看类型定义；
* 检查依赖关系；
* 运行不会修改项目状态的诊断命令。

Research 必须优先确认真实代码行为，不得仅依赖文件名、注释、README 或历史 Plan 推断当前实现。

---

## 2. Plan

Plan 必须建立在 Research 结果之上。

Plan 应明确回答：

* 改什么；
* 为什么改；
* 怎么改；
* 涉及哪些文件；
* 是否影响现有接口或行为；
* 有哪些风险；
* 如何验证改动成功。

Plan 文件至少应包含：

* 目标和成功标准；
* 架构或逻辑设计；
* 关键流程；
* 受影响文件列表；
* 每个文件计划修改的内容；
* 兼容性和破坏性变更评估；
* 风险和边界情况；
* 验证方案；
* 注意事项与技术折衷；
* Todo List。

必要时可以加入：

* 关键接口定义；
* 数据结构示例；
* 伪代码；
* 小型代码片段。

Plan 不要求提前编写完整实现代码。

### 注释循环

Plan 完成后进入用户 Review：

`Plan → 用户审查 / 内联注释 → 更新 Plan → 再次 Review`

循环直到用户明确批准。

### 禁止提前实施

当用户表达以下含义时：

* `don't implement yet`
* `先别写代码`
* `只更新计划`
* `先改 Plan`
* 其他明确要求暂缓实施的表达

无论 Plan 是否已经完成，都禁止修改产品代码。

### 实施授权

只有在用户明确表达实施意图后才能进入 Implement，例如：

* `implement it all`
* `开始实施`
* `按这个计划执行`
* `直接做吧`
* `按 Plan 全部实现`

类似“看起来不错”“可以”“差不多”但没有明确实施含义的表达，不应自动视为实施授权。

### Todo List

Plan 文件末尾必须维护 Todo List：

```markdown
## Todo

- [ ] Task A
- [ ] Task B
- [ ] Task C
```

Todo 应对应可验证的实施任务，而不是泛泛描述。

---

## 3. Implement

实施阶段必须严格围绕已批准的 Plan 执行。

如果实施过程中发现 Plan 存在明显错误、遗漏或新的重要技术约束：

1. 暂停相关实现；
2. 说明发现的问题；
3. 更新 Plan；
4. 涉及重要设计变化时重新获取用户批准。

不得为了“完成 Plan”而强行实现已经确认错误的方案。

### 执行标准

* 完整执行用户批准范围内的计划；
* 每完成一个 Todo 后及时打勾；
* 禁止遗留无意义占位符，例如 `// TODO`、`// ...`；
* 必须显式处理错误，不得无理由吞掉 Error；
* 保持代码简洁；
* 不增加与当前需求无关的抽象；
* 不进行计划外的大范围重构；
* 优先复用项目现有能力；
* 遵循当前代码库已有风格；
* 修改范围应尽可能精确。


### 错误处理

禁止出现无理由的静默错误，例如：

```ts
try {
  ...
} catch {
}
```

错误必须根据场景进行：

* 显式返回；
* 转换为业务错误；
* 日志记录；
* 上抛；
* 用户反馈；
* 合理降级。

不得通过吞掉异常掩盖实现问题。

---

## 代码库探索与修改规则

### 修改前先搜索

修改任何核心逻辑前，必须搜索：

* 对应函数或组件的调用位置；
* 相关类型定义；
* 已有工具函数；
* 类似业务实现；
* 相关测试；
* 相关配置；
* 可能存在的历史兼容逻辑。

不得看到第一个相关文件后立即开始修改。

### 优先复用

创建新的以下内容前，应先搜索项目是否已有同类实现：

* Component；
* Hook；
* Utility；
* Service；
* Type；
* Schema；
* API Wrapper；
* Config；
* 常量；
* 数据转换逻辑。

如果已有能力可以合理复用，应优先复用，而不是创建重复实现。

### 调用链检查

修改以下内容时，必须检查所有主要调用方：

* 公共函数；
* API；
* Type；
* Schema；
* 数据结构；
* Shared Component；
* Context；
* Store；
* Service。

不能只保证定义处类型正确。

### 不做投机性重构

除非当前需求明确需要，否则不得：

* 顺便重构无关代码；
* 提前抽象未来可能需要的能力；
* 创建没有当前调用方的通用框架；
* 大规模统一风格；
* 顺手替换技术栈。

---

## 4. Verify

代码修改完成不代表任务完成。

实施结束后必须进行验证。

### Diff 检查

首先检查最终 `git diff`，确认：

* 所有修改都属于当前计划；
* 不存在意外文件修改；
* 不存在调试代码；
* 不存在临时日志；
* 不存在无意义格式化；
* 不存在未完成占位符。

### 自动验证

根据项目能力执行适用的验证，例如：

* typecheck；
* lint；
* unit test；
* integration test；
* build；
* 受影响模块的专项测试。

完成一个独立修改单元后，可以优先执行局部验证。

整个实施结束前，应执行当前项目合理可行的完整验证。

不得机械地在每次微小编辑后运行全量检查。

### 行为验证

对于用户可见功能，应尽可能验证真实行为，而不仅是类型和编译结果。

例如：

* 页面能够正常打开；
* 交互行为符合预期；
* API 请求结果正确；
* 错误状态正常；
* 空状态正常；
* 原有关键流程没有被破坏。

### Todo 对照

逐项检查 Plan 中的 Todo List。

只有确认完成并验证后才能标记：

```markdown
- [x] Task
```

不得仅因为“代码已经写了”就视为完成。

### 无法验证的情况

如果某项验证由于环境、依赖、权限或外部服务无法执行，必须明确说明：

* 哪项没有验证；
* 为什么无法验证；
* 已完成哪些替代检查；
* 仍存在什么风险。

不得在存在未验证内容时宣称“全部验证通过”。

---

## CHANGELOG 规则

`CHANGELOG.md` 用于记录对项目有实际意义的变化，而不是记录所有编辑操作。

以下变化原则上需要记录：

* 用户可感知的新功能；
* Bug 修复；
* 核心行为变化；
* API 或数据结构变化；
* 重要架构调整；
* 依赖或构建方式变化；
* 重要性能或稳定性改进。

以下变化原则上无需记录：

* 单纯代码格式调整；
* 注释修改；
* 文档错别字；
* 不改变行为的内部重构；
* 测试代码整理；
* import 排序；
* 其他无用户或工程影响的微小修改。

用户可以明确要求某次改动必须或无需写入 CHANGELOG。

### 格式

使用三级标题：

```markdown
### YYYY-MM-DD

- 修改内容 A
- 修复问题 B
```

同一天的多次改动必须追加到同一个日期标题下，不得重复创建相同日期标题。

CHANGELOG 必须精简，只记录“改了什么”，避免：

* 实现过程；
* 背景复述；
* Research 内容；
* Plan 内容；
* 重复描述；
* 无意义技术细节。

---

## 用户反馈与范围变化

### 简短反馈

对于实现过程中的局部反馈，例如：

* 文案调整；
* 间距修改；
* 轻微交互修改；
* 小范围样式调整；

如果不改变原 Plan 的核心设计，可直接调整并继续验证。

### 重大变化

如果用户反馈改变了：

* 核心需求；
* 技术架构；
* 数据结构；
* API；
* 实施范围；
* 关键交互逻辑；

则暂停原实施，重新评估是否需要更新 Research / Plan。

### 回滚指令

收到明确回滚指令时：

1. 停止继续执行原计划；
2. 确认需要回滚的范围；
3. 专注执行回滚或新的需求；
4. 不擅自恢复原计划。

### 用户决策权

用户拥有最终决策权，可以：

* 选择性执行 Plan；
* 删除部分 Todo；
* 缩减改动范围；
* 要求保护现有接口；
* 覆盖技术方案；
* 改变优先级；
* 要求停止实施。

---

## 工作模式

优先在单个长会话内完成完整闭环：

`深度阅读 → Research → Plan → 用户 Review → Plan 更新 → 用户批准 → Implement → Verify`

目标是减少上下文丢失，同时让重要决策有明确记录。

不得为了追求流程完整而制造没有实际价值的文档或步骤。

---

## 行为准则

1. **编码前思考**

   明确假设、约束和不确定点，不隐藏影响实现的重要问题。

2. **简单优先**

   使用能够满足当前需求的最简单可靠方案，不进行投机性抽象。

3. **精确修改**

   只修改完成当前目标所必要的部分，并匹配项目已有风格。

4. **目标驱动**

   在实施前定义可验证的成功标准，并持续验证直到达到标准。

5. **事实优先**

   对当前代码行为的判断必须基于实际代码、调用链和运行结果，不得只依赖 README、注释或历史文档。

6. **复用优先**

   新建能力之前先寻找已有实现，避免重复组件、重复工具和重复业务逻辑。

7. **避免隐式扩展范围**

   发现额外问题时可以说明，但不得未经用户同意顺手扩大改动范围。

---

## 陷阱与注意事项

### 1. 工具写入验证：Edit / Write 预览不等于最终磁盘内容

已观察到部分环境下，Edit 或 Write 工具返回的修改预览与随后 Read 得到的实际文件内容可能不一致。

因此，不得仅根据 Edit / Write 的返回结果判断修改已经真实落盘。

### 强制流程

任何重要 Edit / Write 操作完成后，必须重新读取改动位置进行确认。

可以只读取改动附近的片段，不要求每次重新读取完整文件。

如果重新读取后仍然是旧内容：

* 说明本次写入没有真实生效；
* 禁止将该任务标记为完成；
* 改用当前环境中更可靠的文件写入方式；
* 写入后再次读取确认。

Windows 环境下，如普通 Edit / Write 无法可靠落盘，可使用 PowerShell `.NET File API`：

```powershell
$utf8NoBom = New-Object System.Text.UTF8Encoding $false
[System.IO.File]::WriteAllText($path, $content, $utf8NoBom)
```

写入完成后必须重新读取文件进行验证。

---

## 最终完成标准

任务只有在同时满足以下条件后才能视为完成：

* 已批准范围内的 Todo 已完成；
* 实际修改与 Plan 基本一致；
* `git diff` 已检查；
* 必要的 typecheck / lint / test / build 已执行；
* 用户可见行为在条件允许时已验证；
* 不存在未说明的验证失败；
* CHANGELOG 已按规则处理；
* 不存在计划外修改；
* 不存在遗留占位代码。

最终回复应简洁说明：

* 实际完成了什么；
* 关键修改位置；
* 验证结果；
* 是否存在未验证项或剩余风险。

---

**准则生效标志**：计划外改动更少、重复实现更少、重要问题在编码前暴露、最终结果可验证，且 Research / Plan 不沦为形式化流程。
