# Changelog

### 2026-09-05

- 增强无 API 的 Demo 决策辅助：根据偏好解释取舍，记录候选补充与顾虑，承接关键追问并支持不修改当前条件的假设比较。
- 模型理解和解释接入对话历史与候选依据，校验原文纠正和引用，失败时明确回退；侧栏优先展示当前倾向、依据、代价和本轮变化。

### 2026-09-04

- 修复通用 Demo 仍进入旧工作台的问题，四类示例统一使用对话及可编辑侧栏，旧记录保留本地。
- 新增 Docker、Render 与部署文档，支持将现有 ASGI 应用部署为网页服务。
- 将饮食对话与可编辑结果侧栏推广至购物、旅行及通用选择，保留详细比较和候选编辑。
- 统一后台场景识别，支持文字候选、条件来源与确认、预算筛选和偏好权重；通用会话支持幂等重试、历史恢复及版本冲突处理。
- 饮食聊天新增可编辑决策侧栏与移动抽屉，支持条件确认、替换、清空和三餐结果展示。
- 饮食对话与面板同步保存，支持会话恢复、请求幂等和版本冲突恢复；保护已确认条件不被模型推断覆盖。
- 从 Diet 主干抽取共享 StageRunner，Diet、Travel、Shopping 与 Generic 复用统一调度和排序。
- 增加服务端工作台命令、评分明细、手工候选与搜索来源；真实搜索配置独立，失败不再静默切换本地 demo。
- 修复 context/目标/owner 丢失、跨入口 revision 不一致、并发覆盖、Diet 权重泄漏、硬约束和缺失值评分问题。
- 保留 Diet 三餐计划与健康风险拦截，补齐编辑回归测试和购物类别隔离。

### 2026-09-03

- 新增通用决策后端链路，支持通过 `/api/v1/decisions` 创建并持久化非饮食决策。
- 新增 Domain Plugin 和 Domain Registry 基础设施，并接入旅行 fixture 领域和饮食领域适配声明。
- 首页非饮食输入优先进入后端通用决策结果页，后端不可用时保留本地 demo 回退。

### 2026-08-31

- 补齐饮食评估核心指标，新增槽位准确率、延迟、成本、fallback、安全、幻觉控制、多轮一致性和反馈归因评分。

### 2026-08-30

- 新增前端设置页，可配置浏览器侧模型 API，并在未配置时显示演示模式。
- 通用演示模式新增约束与候选项准备、编辑和确认流程。
- 新增通用演示模式，支持旅行、Offer、学习路径和购物决策的 fixture 工作台。
- 统一 Choice Agent 用户入口，移除用户侧“饮食模式”并列导航。
- 首页饮食类问题会直接进入决策助手并自动发起第一轮对话。
- 将餐食库下沉为“我的数据”辅助入口，并弱化聊天页数据源模式感。

### 2026-08-29

- 新建 Python Choice Agent V2，并将饮食作为首个完整决策领域。
- 迁移饮食推荐、澄清、调整、三餐计划、餐食库、反馈、Trace 和评估能力。
- 建立多 Agent 编排、通用 DecisionState、证据模型和确定性决策引擎。
- 迁移旧饮食种子数据、提示词、兼容 API 和前端页面。
- 首页改为通用决策入口，并将饮食能力收拢为独立模式入口。
- 收紧首页模块、字体和表单尺寸，提升不同浏览器高度下的首屏适配。
- 补齐 GitHub 发布所需的忽略规则、MIT 许可证和项目元信息。
- 补强通用决策状态、状态流转和 revision 校验，并将饮食聊天流程接入该核心能力。
- 新增通用候选选择器，并将饮食推荐接入随机、匹配度加权、很久未推荐优先和近期冷却策略。
- 新增浅绿色与活力橙两套前端皮肤，并支持本地记忆主题选择。
